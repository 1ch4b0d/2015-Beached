using UnityEngine;
using System.Collections;
using Acrocatic;

// Ugh. This script was creaed in order be a lazy solution so that sound can
// be filtered on player collision. This script should be added to any object
// that the player collides with in order to make the landing sound
// I hate acrocatic so much. The writer.... of that.... ugh....
public class PlatformCollider : MonoBehaviour {
    // Use this for initialization
    protected virtual void Start() {
    }
    
    // Update is called once per frame
    protected virtual void Update() {
    }
}